{{- if and .Values.serviceMonitor.enabled .Values.serviceMonitor.certManager.enabled (not .Values.metrics.certificates.enabled) }}
{{- fail "serviceMonitor.certManager.enabled requires metrics.certificates.enabled so the manager serves the same certificate that ServiceMonitor verifies" }}
{{- end }}
