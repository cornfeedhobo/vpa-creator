{{- if and .Values.metrics.enabled .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "vpa-creator.fullname" . }}-allow-metrics-traffic
  namespace: {{ include "vpa-creator.namespace" . }}
  labels:
    {{- include "vpa-creator.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "vpa-creator.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            {{- toYaml .Values.networkPolicy.namespaceSelector | nindent 12 }}
      ports:
        - port: {{ .Values.metrics.service.targetPort }}
          protocol: TCP
{{- end }}
